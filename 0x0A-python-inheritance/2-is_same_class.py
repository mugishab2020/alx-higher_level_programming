#!/usr/bin/python3
""" thiss is the function t check if  obj is for the class"""


def is_same_class(obj, a_class):
    """ the inside of the function """
    if type(obj) == a_class:
        return True
    return False
