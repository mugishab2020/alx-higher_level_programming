#!/usr/bin/python3
""" JSON deals are on"""
import json
"""Now we are on with json in pyhton"""


def to_json_string(my_obj):
    """ here is the function for JSON representation for
    an object as arrgument"""
    return json.dumps(my_obj)
